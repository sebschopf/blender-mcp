# 0018 - Archive → implementation mapping

Résumé
-------
Créer un changement qui formalise le mapping entre symboles archivés (dans root `blender_mcp` shim) et les nouvelles implémentations sous `src/blender_mcp`. Fournir une table de dispositions et recommandations de migration.

Type
----
NEW SPEC: documentation + delta spec pour 0004 archive-symbols.
