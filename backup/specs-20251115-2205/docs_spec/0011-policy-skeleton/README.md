# 0011 — Policy skeleton (POC)

But
---
Fournir un POC minimal `src/blender_mcp/policy.py` qui charge une petite policy YAML et expose `check(action, context)`.

Livrables
--------
- `src/blender_mcp/policy.py` (POC)
- `tests/test_policy_basic.py`
