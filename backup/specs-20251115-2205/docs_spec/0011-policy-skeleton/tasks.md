# Tasks — 0011 Policy skeleton

1. Implementer `src/blender_mcp/policy.py` avec API minimale `load_policy(yaml_blob)` et `check(action, context)`.
2. Ajouter tests unitaires `tests/test_policy_basic.py` couvrant allow/deny cases.
3. Add sample policy YAML in `tests/fixtures` for tests.
