# spec — 0011-policy-skeleton (new)

ADDED:
- Requirement: A minimal policy POC module must be provided with API `load_policy` and `check`.
- Requirement: Unit tests must demonstrate allow/deny behavior for at least two rule examples.
