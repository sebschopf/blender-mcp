# 0017 - CI: OpenSpec validate & E2E harness job

Résumé
-------
Ajouter des définitions de job CI pour valider automatiquement `openspec validate --strict` et optionnellement exécuter l'E2E harness en mode mock.

Type
----
NEW SPEC: CI job definitions + tasks.
