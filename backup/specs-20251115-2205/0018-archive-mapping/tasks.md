# Tasks — 0018 Archive mapping

1. Generate `docs/archive_symbol_mapping.md` via script/manual mapping.
2. For each symbol, recommend: keep shim + deprecate, move and add shim, or remove.
3. If moves are required, create OpenSpec delta(s) documenting API changes.
