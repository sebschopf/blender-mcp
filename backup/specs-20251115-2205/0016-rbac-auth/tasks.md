# Tasks — 0016 RBAC & auth

1. Draft role definitions and scope mapping.
2. Add a small prototype token check hooking into dispatcher for `start-server` and `execute` actions.
3. Add tests describing allowed/denied cases.
