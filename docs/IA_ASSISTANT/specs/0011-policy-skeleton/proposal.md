# 0011 - Policy skeleton implementation (POC)

Résumé
-------
Preuve de concept (POC) d'un petit module `policy.py` exposant une API `load_policy()` et `check(action, context)`.

Type
----
NEW SPEC: implémentation POC qui réalise une partie des tâches décrites dans 0005 et 0010.
