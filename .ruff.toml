# Ruff configuration for the project
line-length = 120

# Exclude long/complex helper scripts and docs from strict linting for now.
exclude = ["docs", "scripts", "tools", "src/blender_mcp/archive"]

[lint]
# Lint selection — keep important categories
select = ["E", "F", "I", "C", "R"]
# Tolerate a few style rules that aren't critical for this repo
extend-ignore = ["E203", "E402", "E741"]
