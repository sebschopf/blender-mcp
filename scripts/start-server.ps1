<# Minimal placeholder start-server.ps1 used by the embedded adapter default command.
   This script is intentionally a no-op for CI and tests. Replace with a real
   helper script when implementing the server adapter runtime. #>
Write-Output "start-server placeholder: no-op"
exit 0
